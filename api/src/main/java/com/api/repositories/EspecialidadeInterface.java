package com.api.repositories;

import com.api.models.EspecialidadeModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EspecialidadeInterface extends JpaRepository<EspecialidadeModel, Long> {
}
